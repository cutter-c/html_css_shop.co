---
const { name, price, star, img } = Astro.props
---

<div class="product-item">
  <img src={img} alt={name}>
  <h3>{name}</h3>
  <section>
    ⭐⭐⭐⭐⭐
    <strong>{star}/5</strong>
  </section>
  <small>{price}</small>
</div>

<style>
  .product-item {
    display: flex;
    flex-direction: column;
    img {
      border-radius: 1em;
      max-width: 100%;
    }
    h3 {
      font-size: 1em;
      font-weight: bold;
      /* margin-top: 0.5em; */
    }
    strong {
      display: inline-block;
      margin-top: 0em;
    }
  }
</style>